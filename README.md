# 🧹 Data Cleaner App

A simple **Streamlit web application** for cleaning CSV data files.  
You can upload a dataset, remove or replace missing/blank values, and download a cleaned version instantly.

---

## 🚀 Features
- Upload any CSV file
- Detect missing and blank values
- Choose to:
  - Remove rows with missing/blank values
  - Fill missing/blank cells with `"NA"`
- Automatically remove duplicates
- Preview before and after cleaning
- Download the cleaned dataset
- Saves locally as `cleaned_data_output.csv`

---

## 🧩 How to Run

1. Clone this repository:
   ```bash
   git clone https://github.com/<your-username>/Data_Cleaner_App.git
   cd Data_Cleaner_App
